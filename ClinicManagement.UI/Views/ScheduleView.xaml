<UserControl x:Class="ClinicManagement.UI.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000" Background="#F1F5F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="XẾP LỊCH LÀM VIỆC" FontSize="28" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding ClonePreviousWeekCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#3B82F6" BorderBrush="#3B82F6" Height="40" Padding="15,0" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentCopy" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="SAO CHÉP TUẦN TRƯỚC" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignFlatLightBgButton}" Background="#10B981" Foreground="White" Height="40" Padding="20,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="LƯU LỊCH" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding PreviousWeekCommand}" Style="{StaticResource MaterialDesignIconButton}" Foreground="#64748B">
                    <materialDesign:PackIcon Kind="ChevronLeft" Width="24" Height="24"/>
                </Button>
                <TextBlock Text="{Binding WeekDisplayTitle}" FontSize="18" FontWeight="SemiBold" Foreground="#334155" VerticalAlignment="Center" Margin="15,0"/>
                <Button Command="{Binding NextWeekCommand}" Style="{StaticResource MaterialDesignIconButton}" Foreground="#64748B">
                    <materialDesign:PackIcon Kind="ChevronRight" Width="24" Height="24"/>
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20" Effect="{StaticResource MaterialDesignShadowDepth1}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="NHÂN VIÊN" FontWeight="Bold" FontSize="14" Foreground="#475569" Padding="10"/>
                        
                        <UniformGrid Grid.Column="1" Rows="1" Columns="7">
                            <TextBlock Text="Thứ 2" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Thứ 3" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Thứ 4" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Thứ 5" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Thứ 6" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Thứ 7" FontWeight="Bold" FontSize="14" Foreground="#475569" HorizontalAlignment="Center"/>
                            <TextBlock Text="Chủ nhật" FontWeight="Bold" FontSize="14" Foreground="#EF4444" HorizontalAlignment="Center"/>
                        </UniformGrid>
                    </Grid>

                    <Separator Grid.Row="0" VerticalAlignment="Bottom" Background="#E2E8F0"/>

                    <!-- Body -->
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding EmployeeRows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F1F5F9" BorderThickness="0,0,0,1" Padding="0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="200"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="10,0">
                                            <TextBlock Text="{Binding Employee.FullName}" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Employee.Role}" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                                        </StackPanel>

                                        <ItemsControl Grid.Column="1" ItemsSource="{Binding Days}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <UniformGrid Rows="1" Columns="7"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="#E2E8F0" BorderThickness="1" Margin="2" Padding="5" Background="#F8FAFC" CornerRadius="4">
                                                        <ItemsControl ItemsSource="{Binding Shifts}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <CheckBox Content="{Binding Shift.Name}" IsChecked="{Binding IsSelected}" Margin="0,2"/>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
