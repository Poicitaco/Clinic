<UserControl x:Class="ClinicManagement.UI.Views.PersonalSalaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="950"
             Background="#F1F5F9">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="THÔNG TIN LƯƠNG" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="{Binding CurrentPeriodText}" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Kỳ hiện tại">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,16">
                        <Border Background="White" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Tổng giờ" Foreground="#64748B"/>
                                <TextBlock Text="{Binding TotalHours, StringFormat=N2}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Hệ số ca mặc định" Foreground="#64748B"/>
                                <TextBlock Text="{Binding DefaultShiftCoefficient, StringFormat=N2}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Hệ số Lễ tân" Foreground="#64748B"/>
                                <TextBlock Text="{Binding ReceptionistCoefficient, StringFormat=N2}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="Tiền theo ca" Foreground="#64748B"/>
                                <TextBlock Text="{Binding TotalShiftAmount, StringFormat=N0}" FontSize="20" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Background="White" CornerRadius="8" Padding="14">
                            <StackPanel>
                                <TextBlock Text="Tổng lương" Foreground="#64748B"/>
                                <TextBlock Text="{Binding TotalAmount, StringFormat=N0}" FontSize="20" FontWeight="Bold" Foreground="#10B981"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding CurrentSalaryMessage}" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentShiftRows}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Background="White" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="NGÀY" Binding="{Binding ScheduleDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                                    <DataGridTextColumn Header="CA" Binding="{Binding ShiftName}" Width="*"/>
                                    <DataGridTextColumn Header="SỐ GIỜ CA" Binding="{Binding ShiftHours, StringFormat=N2}" Width="110"/>
                                    <DataGridTextColumn Header="HỆ SỐ CA" Binding="{Binding ShiftCoefficient, StringFormat=N2}" Width="110"/>
                                    <DataGridTextColumn Header="HS BỆNH NHÂN" Binding="{Binding PatientCoefficient, StringFormat=N2}" Width="130"/>
                                    <DataGridTextColumn Header="GIỜ QUY ĐỔI" Binding="{Binding ConvertedHours, StringFormat=N2}" Width="130"/>
                                    <DataGridTextColumn Header="TIỀN/GIỜ" Binding="{Binding HourlyRate, StringFormat=N0}" Width="120"/>
                                    <DataGridTextColumn Header="TIỀN CA" Binding="{Binding Amount, StringFormat=N0}" Width="120"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Lịch sử lương">
                <Grid Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="14" Margin="0,0,0,16" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tổng lương đã chốt: " Foreground="#64748B" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding LifetimeFinalizedSalary, StringFormat=N0}" FontSize="18" FontWeight="Bold" Foreground="#10B981"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding SalaryHistoryMessage}" Foreground="#64748B" FontWeight="SemiBold" Margin="0,0,0,12"/>
                            <DataGrid Grid.Row="1" ItemsSource="{Binding SalaryHistoryRows}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="48" Background="White" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="THÁNG" Binding="{Binding Month}" Width="100"/>
                                    <DataGridTextColumn Header="NĂM" Binding="{Binding Year}" Width="100"/>
                                    <DataGridTextColumn Header="TỔNG GIỜ" Binding="{Binding TotalHours, StringFormat=N2}" Width="130"/>
                                    <DataGridTextColumn Header="TỔNG LƯƠNG" Binding="{Binding TotalAmount, StringFormat=N0}" Width="160"/>
                                    <DataGridTextColumn Header="NGÀY CHỐT" Binding="{Binding FinalizedDate, StringFormat=dd/MM/yyyy HH:mm}" Width="180"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
