<Window x:Class="ClinicManagement.UI.Views.EmployeeFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Thông tin nhân viên" SizeToContent="Height" Width="550" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="HỒ SƠ NHÂN SỰ" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,30"/>

        <!-- Error Message -->
        <TextBlock Grid.Row="1" Text="{Binding Error}" Foreground="#EF4444" HorizontalAlignment="Center" Margin="0,0,0,15" FontWeight="Medium"/>

        <!-- Họ và Tên -->
        <TextBox Grid.Row="2" materialDesign:HintAssist.Hint="Họ và tên *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Margin="0,0,0,25" FontSize="16" Foreground="#0F172A"/>

        <Grid Grid.Row="3" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DatePicker Grid.Column="0" materialDesign:HintAssist.Hint="Ngày sinh *" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" SelectedDate="{Binding DateOfBirth, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" FontSize="16" Foreground="#0F172A"/>
            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Giới tính *" Style="{StaticResource MaterialDesignFloatingHintComboBox}" ItemsSource="{Binding Genders}" SelectedItem="{Binding Gender}" FontSize="16" Foreground="#0F172A">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Số điện thoại *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" FontSize="16" Foreground="#0F172A"/>
            <TextBox Grid.Column="2" materialDesign:HintAssist.Hint="Email *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" FontSize="16" Foreground="#0F172A"/>
        </Grid>

        <Grid Grid.Row="5" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Vai trò *" Style="{StaticResource MaterialDesignFloatingHintComboBox}" ItemsSource="{Binding Roles}" SelectedItem="{Binding Role}" FontSize="16" Foreground="#0F172A">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <DatePicker Grid.Column="2" materialDesign:HintAssist.Hint="Ngày bắt đầu *" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Foreground="#0F172A"/>
        </Grid>

        <Grid Grid.Row="6" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" materialDesign:HintAssist.Hint="Học vị" Style="{StaticResource MaterialDesignFloatingHintComboBox}" ItemsSource="{Binding Degrees}" SelectedItem="{Binding Degree, ValidatesOnDataErrors=True}" IsEnabled="{Binding IsDegreeEnabled}" VerticalAlignment="Top" FontSize="16" Foreground="#0F172A">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Trạng thái" Style="{StaticResource MaterialDesignFloatingHintComboBox}" ItemsSource="{Binding ContractStatuses}" SelectedItem="{Binding ContractStatus}" VerticalAlignment="Top" Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="16" Foreground="#0F172A">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="HỦY" Style="{StaticResource MaterialDesignOutlinedButton}" Width="100" Height="42" Margin="0,0,15,0" Click="CancelButton_Click" Foreground="#64748B" BorderBrush="#CBD5E1"/>
            <Button Content="LƯU THÔNG TIN" Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Width="160" Height="42" Background="#38BDF8" BorderBrush="#38BDF8" Foreground="White" FontWeight="Bold" materialDesign:ButtonAssist.CornerRadius="8"/>
        </StackPanel>
    </Grid>
</Window>
