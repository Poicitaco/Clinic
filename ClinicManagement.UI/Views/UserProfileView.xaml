<UserControl x:Class="ClinicManagement.UI.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White">
    <ScrollViewer>
        <StackPanel Margin="30" MaxWidth="800">
            <TextBlock Text="Hồ Sơ Cá Nhân" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <!-- Cập nhật thông tin -->
            <materialDesign:Card Padding="25" Margin="0,0,0,30">
                <StackPanel>
                    <TextBlock Text="Thông tin cơ bản" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBox Grid.Column="0" Grid.Row="0" materialDesign:HintAssist.Hint="Họ và tên" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding FullName, Mode=OneWay}" IsReadOnly="True" />
                        <TextBox Grid.Column="2" Grid.Row="0" materialDesign:HintAssist.Hint="Vai trò" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Role, Mode=OneWay}" IsReadOnly="True" />

                        <TextBox Grid.Column="0" Grid.Row="2" materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Email, Mode=OneWay}" IsReadOnly="True" />
                        <TextBox Grid.Column="2" Grid.Row="2" materialDesign:HintAssist.Hint="Số điện thoại" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding PhoneNumber, Mode=OneWay}" IsReadOnly="True" />
                    </Grid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
