<UserControl x:Class="ClinicManagement.UI.Views.SalaryConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="#F1F5F9">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CẤU HÌNH LƯƠNG" FontSize="28" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
            <Button Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignFlatLightBgButton}" Background="#10B981" Foreground="White" HorizontalAlignment="Right" Height="40" Padding="20,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentSave" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="LƯU CẤU HÌNH" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Effect="{StaticResource MaterialDesignShadowDepth1}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,24">
                    <TextBox materialDesign:HintAssist.Hint="Số tiền một giờ" Text="{Binding Configuration.HourlyRate, UpdateSourceTrigger=PropertyChanged, StringFormat=N0}" Margin="0,0,12,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <TextBox materialDesign:HintAssist.Hint="Hệ số ca mặc định" Text="{Binding Configuration.DefaultShiftCoefficient, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                    <TextBox materialDesign:HintAssist.Hint="Hệ số nhân viên Lễ tân" Text="{Binding Configuration.ReceptionistCoefficient, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                </UniformGrid>

                <TextBlock Grid.Row="1" Text="Hệ số trình độ Nha sĩ" FontSize="18" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,12"/>

                <DataGrid Grid.Row="2" ItemsSource="{Binding DentistDegreeCoefficients}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" BorderThickness="0" RowHeight="54">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Học vị" Binding="{Binding Degree}" IsReadOnly="True" Width="220" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                        <DataGridTextColumn Header="Hệ số trình độ" Binding="{Binding Coefficient}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
