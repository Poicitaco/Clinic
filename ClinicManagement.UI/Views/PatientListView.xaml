<UserControl x:Class="ClinicManagement.UI.Views.PatientListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="20,10,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Danh Sách Bệnh Nhân" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Quản lý thông tin cá nhân của bệnh nhân" FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>
            </StackPanel>
            <Button Content="+ THÊM BỆNH NHÂN" Command="{Binding AddCommand}" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Right" VerticalAlignment="Center" Height="42" Background="#38BDF8" BorderBrush="#38BDF8" Foreground="White" FontWeight="Bold" materialDesign:ButtonAssist.CornerRadius="8" Padding="20,0"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <TextBox Width="260" HorizontalAlignment="Left" materialDesign:HintAssist.Hint="Tìm theo tên hoặc SĐT" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="0">
            <DataGrid Margin="5" ItemsSource="{Binding Patients}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="Transparent" BorderThickness="0" RowHeight="55" AlternatingRowBackground="#F8FAFC" HorizontalGridLinesBrush="#E2E8F0" VerticalGridLinesBrush="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="HỌ TÊN" Binding="{Binding FullName}" Width="*" FontWeight="SemiBold" Foreground="#0F172A"/>
                    <DataGridTextColumn Header="SĐT" Binding="{Binding PhoneNumber}" Width="120"/>
                    <DataGridTextColumn Header="NGÀY SINH" Binding="{Binding DateOfBirth, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="GIỚI TÍNH" Binding="{Binding Gender}" Width="100"/>
                    <DataGridTextColumn Header="EMAIL" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="ĐỊA CHỈ" Binding="{Binding Address}" Width="*"/>
                    <DataGridTemplateColumn Header="" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="SỬA" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignOutlinedButton}" Height="32" Padding="15,0" Foreground="#38BDF8" BorderBrush="#38BDF8" materialDesign:ButtonAssist.CornerRadius="6"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
