<Window x:Class="ClinicManagement.UI.Views.WorkShiftFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="{DynamicResource MaterialDesignFont}" Background="#F8FAFC">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,30" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1">
            <TextBox materialDesign:HintAssist.Hint="Tên ca (VD: Ca Sáng) *" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" FontSize="16"/>
            
            <materialDesign:TimePicker materialDesign:HintAssist.Hint="Giờ bắt đầu *" SelectedTime="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignFloatingHintTimePicker}" Margin="0,0,0,20" FontSize="16" Is24Hours="True"/>
            
            <materialDesign:TimePicker materialDesign:HintAssist.Hint="Giờ kết thúc *" SelectedTime="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignFloatingHintTimePicker}" Margin="0,0,0,20" FontSize="16" Is24Hours="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" Content="LƯU" Style="{StaticResource MaterialDesignFlatLightBgButton}" Background="#3B82F6" Foreground="White" Width="120" Height="45" Margin="0,0,16,0"/>
            <Button Command="{Binding CancelCommand}" Content="HỦY" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#64748B" BorderBrush="#E2E8F0" Width="120" Height="45"/>
        </StackPanel>
    </Grid>
</Window>
