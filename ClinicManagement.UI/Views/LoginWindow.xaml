<Window x:Class="ClinicManagement.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Đăng nhập - Quản lý phòng khám" Height="550" Width="850" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" AllowsTransparency="True">
    <Border CornerRadius="12" Background="White" BorderBrush="LightGray" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Branding / Illustration -->
            <Border Grid.Column="0" CornerRadius="12,0,0,12" Background="{DynamicResource PrimaryHueMidBrush}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
                    <materialDesign:PackIcon Kind="ToothOutline" Width="100" Height="100" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="NhaKhoa Pro" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Giải pháp quản lý phòng khám hiện đại, toàn diện và chuyên nghiệp." FontSize="16" Foreground="White" TextWrapping="Wrap" TextAlignment="Center" Opacity="0.9"/>
                </StackPanel>
            </Border>

            <!-- Right Panel: Login Form -->
            <Grid Grid.Column="1" Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource MaterialDesignIconForegroundButton}" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Click="CloseButton_Click" ToolTip="Đóng">
                    <materialDesign:PackIcon Kind="Close" />
                </Button>

                <TextBlock Grid.Row="1" Text="Đăng nhập" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,10,0,5"/>
                <TextBlock Grid.Row="2" Text="Vui lòng đăng nhập để tiếp tục" FontSize="14" Foreground="Gray" Margin="0,0,0,30"/>

                <!-- Email -->
                <TextBox Grid.Row="3" materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" FontSize="16">
                    <materialDesign:HintAssist.FloatingOffset>
                        <Point X="0" Y="-20"/>
                    </materialDesign:HintAssist.FloatingOffset>
                </TextBox>

                <!-- Password -->
                <PasswordBox Grid.Row="4" x:Name="txtPassword" materialDesign:HintAssist.Hint="Mật khẩu" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,20" FontSize="16" PasswordChanged="PasswordBox_PasswordChanged">
                    <materialDesign:HintAssist.FloatingOffset>
                        <Point X="0" Y="-20"/>
                    </materialDesign:HintAssist.FloatingOffset>
                </PasswordBox>

                <!-- Error Message -->
                <TextBlock Grid.Row="5" Text="{Binding ErrorMessage}" Foreground="Red" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,20" Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Login Button -->
                <Button Grid.Row="6" Content="ĐĂNG NHẬP" Command="{Binding LoginCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Height="45" FontSize="16" FontWeight="Bold" Margin="0,10,0,0" IsDefault="True"/>

                <!-- Demo Accounts Hint -->
                <StackPanel Grid.Row="7" Margin="0,20,0,0">
                    <TextBlock Text="Tài khoản Demo:" FontWeight="SemiBold" FontSize="13" Foreground="Gray"/>
                    <TextBlock Text="• admin@clinic.com / Manager@123 (Quản lý)" FontSize="12" Foreground="Gray" Margin="0,2,0,0"/>
                    <TextBlock Text="• dentist@clinic.com / Manager@123 (Nha sĩ)" FontSize="12" Foreground="Gray" Margin="0,2,0,0"/>
                    <TextBlock Text="• receptionist@clinic.com / Manager@123 (Lễ tân)" FontSize="12" Foreground="Gray" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
