<UserControl x:Class="ClinicManagement.UI.Views.SalaryListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="#F1F5F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="BẢNG LƯƠNG" FontSize="28" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tháng:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <TextBox Text="{Binding SelectedMonth}" Width="50" Margin="0,0,20,0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignTextBox}"/>
                
                <TextBlock Text="Năm:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <TextBox Text="{Binding SelectedYear}" Width="80" Margin="0,0,20,0" VerticalAlignment="Center" Style="{StaticResource MaterialDesignTextBox}"/>

                <Button Command="{Binding CalculateCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#3B82F6" BorderBrush="#3B82F6" Height="40" Padding="15,0" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Calculator" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="TÍNH LƯƠNG NHÁP" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <Button Command="{Binding FinalizeCommand}" Style="{StaticResource MaterialDesignFlatLightBgButton}" Background="#EF4444" Foreground="White" HorizontalAlignment="Right" Height="40" Padding="20,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Lock" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="CHỐT LƯƠNG &amp; KHÓA" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20" Effect="{StaticResource MaterialDesignShadowDepth1}">
            <DataGrid ItemsSource="{Binding SalaryRecords}" AutoGenerateColumns="False" IsReadOnly="True" Background="White" BorderThickness="0" RowHeight="60">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã NV" Binding="{Binding EmployeeId}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding Employee.FullName}" Width="200" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTextColumn Header="Số giờ làm" Binding="{Binding TotalHours, StringFormat=N2}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridTextColumn Header="Tổng tiền (VNĐ)" Binding="{Binding TotalAmount, StringFormat=N0}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                    <DataGridCheckBoxColumn Header="Đã chốt" Binding="{Binding IsFinalized}" ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}"/>
                    <DataGridTextColumn Header="Ngày chốt" Binding="{Binding FinalizedDate, StringFormat=dd/MM/yyyy HH:mm}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
